// import { useEnsName, useEnsAvatar } from "wagmi"

export function getEnsAvatar(address, chainId) {

    // const { data: EnsNameData } = useEnsName({
    //     address: address,
    //     chainId: chainId
    // })
    // const { data: EnsAvatarData } = useEnsAvatar({
    //     name: EnsNameData,
    //     chainId: chainId
    // })

    // return (EnsAvatarData)
}